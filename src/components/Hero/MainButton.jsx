
const MainButton = ({buttonText}) => {
  return (
    <div>
        <button className="border border-white px-4 py-2 rounded-lg cursor-pointer relative z-10">{buttonText}</button>
    </div>
  )
}

export default MainButton